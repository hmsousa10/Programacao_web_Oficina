<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0"
         metadata-complete="false">

    <display-name>SGO - Sistema de Gestão de Oficina</display-name>

    <!-- Application initializer -->
    <listener>
        <listener-class>com.oficina.sgo.listener.AppContextListener</listener-class>
    </listener>

    <!-- CORS filter - must run first -->
    <filter>
        <filter-name>CorsFilter</filter-name>
        <filter-class>com.oficina.sgo.filter.CorsFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>CorsFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- JWT authentication filter -->
    <filter>
        <filter-name>JwtFilter</filter-name>
        <filter-class>com.oficina.sgo.filter.JwtFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>JwtFilter</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>

    <!-- Welcome files (frontend) -->
    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>

</web-app>
